<h1>Города <%= link_to "+", new_city_path, :class => 'btn btn-outline-primary' %></h1>

<% @city.each do |city| %>
  <div class="alert alert-light">
    <h3><%= link_to city.name, city_path(city), :class => 'form-control' %></h3>
  </div>
<% end %>