<h1>Страны <%= link_to "+", new_country_path, :class => 'btn btn-outline-primary' %></h1>

<% @country.each do |country| %>
  <div class="alert alert-light">
    <h3><%= link_to country.name, country_path(country), :class => 'form-control' %></h3>
  </div>
<% end %>